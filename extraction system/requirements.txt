# Core runtime dependencies (pip mirror of pyproject.toml).
# Optional extras — see README:
#   OCR (fast mode + raw text fallback):  pip install "paddleocr>=2.7,<3" paddlepaddle
#   Dev/tests:                            pip install pytest
# The AI layer uses the Gemini API (GEMINI_API_KEY env var) via the stdlib
# HTTP client — no API SDK needed. certifi provides the CA bundle.
pydantic>=2.7
certifi>=2024.0
faster-whisper>=1.0
opencv-python>=4.9
imagehash>=4.3
Pillow>=10.0
PyMuPDF>=1.24
